#!/usr/bin/env bash -e

if [ "$#" -lt 2 ]; then
    echo "Usage: nomis-do-to-files pattern command-prefix"
fi

pattern="$1"
shift
command_prefix="$@"

# echo pattern = "${pattern}"
# echo command_prefix = "${command_prefix}"

find . -name "${pattern}" -exec ${command_prefix} {} +
